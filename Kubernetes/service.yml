apiVersion: v1
kind: Service
metadata:
  name: Amazon-app
  labels:
    app: Amazon-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
  selector:
    app: Amazon-app
